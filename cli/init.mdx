---
title: "nowrun init: Token Authentication for CI and Headless Machines"
sidebarTitle: "nowrun init"
description: "nowrun init authenticates the CLI with a publisher token, for CI or any machine without a browser. For normal use, prefer nowrun login."
---

`nowrun init` authenticates the CLI using a publisher token instead of a browser.

<Note>
  For everyday use, prefer [`nowrun login`](/cli/login). It authenticates through the browser with nothing to copy or paste. Reach for `nowrun init` when there is no browser available, such as CI, a remote box, or a container.
</Note>

## Usage

```bash
nowrun init -t <token>
```

## Flags

<ParamField path="-t" name="-t <token>" type="string" required>
  Your nowrun publisher token, from the dashboard at [nowrun.io](https://nowrun.io).
</ParamField>

<ParamField path="--target" name="--target <tool>" type="string">
  Which AI tool to install the skill and MCP server for: `claude` (default), `agents`, `codex`, `copilot`, `cursor`, or `vscode`.
</ParamField>

<ParamField path="-d" name="-d, --dir <path>" type="string">
  Project root for the agent setup. Defaults to the current directory.
</ParamField>

## What it does

1. Validates the token with nowrun.
2. Saves credentials locally so later commands are authenticated.
3. Installs the nowrun skill, which teaches your agent the available commands.
4. Installs the docs MCP server.

That is the same setup `nowrun login` performs; only the authentication step differs.

## Where to find your token

Sign in at [nowrun.io](https://nowrun.io) and copy the token from your dashboard.

<Warning>
  Treat the token like a password. Do not commit it to source control or paste it into shared logs. If it leaks, generate a new one from the dashboard.
</Warning>

## Re-authenticating

`nowrun init -t <token>` is safe to re-run and overwrites stored credentials. `nowrun logout` clears them.
